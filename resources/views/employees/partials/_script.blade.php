{{--
    employees/partials/_script.blade.php
    Data bootstrap only — the employeesData() function is bundled in resources/js/components/employees.js
    and exposed on window via app.js. Blade just needs to pass the base URL.
--}}
{{-- baseUpdateUrl is passed directly as argument in the x-data call in index.blade.php --}}
